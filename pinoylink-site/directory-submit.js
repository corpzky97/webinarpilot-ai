const $=id=>document.getElementById(id);function val(id){return ($(id)?.value||'').trim()}
$('directorySubmitForm').addEventListener('submit',async e=>{e.preventDefault();const form=e.currentTarget,button=$('dirSubmit'),status=$('dirStatus');status.className='form-status';status.textContent='';if(!form.reportValidity())return;button.disabled=true;const old=button.textContent;button.textContent='Submitting…';try{const payload={business_name:val('dirBusiness'),category:val('dirCategory'),island:val('dirIsland'),city:val('dirCity'),email:val('dirEmail'),phone:val('dirPhone'),website:val('dirWebsite'),social_url:val('dirSocial'),address:val('dirAddress'),description:val('dirDescription'),tags:val('dirTags')};const r=await fetch('/api/directory-submit',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload)});const data=await r.json().catch(()=>({}));if(!r.ok){if(r.status===503)throw new Error('Directory submission setup is finishing. Please try again shortly.');throw new Error(data.error||'Unable to submit your listing right now.')}status.className='form-status ok';status.textContent=`Submission received${data.submission_id?` — reference ${data.submission_id}`:''}. PinoyLink will review it before publication.`;form.reset();}catch(err){status.className='form-status err';status.textContent=err.message||'Unable to submit your listing right now.'}finally{button.disabled=false;button.textContent=old}});