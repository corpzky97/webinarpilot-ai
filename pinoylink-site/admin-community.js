let token='',type='lost_found',rows=[],cursor='';const $=id=>document.getElementById(id);const esc=v=>String(v??'').replace(/[&<>\"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[m]));function normalize(d){return d?.data?.data||d?.data||[]}function pageCursor(d){return d?.data?.pagination?.next_cursor||d?.pagination?.next_cursor||''}function fmt(v){if(!v)return '—';try{return new Date(v).toLocaleString()}catch{return v}}function cardLost(r){return `<article class="mod-card"><div class="eyebrow">${esc((r.report_type||'report').toUpperCase())} • ${esc(r.case_id||'No case ID')}</div><h3>${esc(r.item_title||'Untitled item')}</h3><p>${esc(r.description||'')}</p><div class="meta"><span><b>Status</b>${esc(r.review_status||'pending')} / ${esc(r.public_status||'pending')}</span><span><b>Area</b>${esc([r.city_area,r.island].filter(Boolean).join(', ')||'—')}</span><span><b>Date</b>${esc(r.event_date||'—')}</span><span><b>Category</b>${esc(r.item_category||'—')}</span></div><div class="private"><b>Private reporter info</b><br>${esc(r.contact_name||'—')} • ${esc(r.contact_email||'—')} • ${esc(r.contact_phone||'—')}<br><b>Proof prompt:</b> ${esc(r.proof_prompt||'—')}</div>${r.archived_at?`<p class="muted"><b>Archived:</b> ${esc(fmt(r.archived_at))} • ${esc(r.archive_reason||'')}</p>`:''}<div class="actions"><button class="ok" data-a="approve">Approve</button><button class="neutral" data-a="needs_changes">Needs changes</button><button class="warn" data-a="matched">Matched + resolve</button><button class="ok" data-a="returned">Returned + archive</button><button class="neutral" data-a="archive">Archive</button><button class="danger" data-a="reject">Reject</button><button class="danger" data-a="delete">Permanent delete</button></div></article>`}function cardMem(r){return `<article class="mod-card"><div class="eyebrow">MEMORIAL • ${esc(r.memorial_id||'No ID')}</div><h3>${esc(r.full_name||'Unnamed memorial')}</h3><p>${esc(r.life_tribute||'')}</p><div class="meta"><span><b>Status</b>${esc(r.review_status||'pending')} / ${esc(r.public_status||'draft')}</span><span><b>Dates</b>${esc(r.birth_date||'—')} — ${esc(r.death_date||'—')}</span><span><b>Service end</b>${esc(r.service_end_date||'—')}</span><span><b>Source</b>${esc(r.source_type||'—')}</span></div><p><b>Service:</b> ${esc(r.service_details||'—')}<br><b>Location:</b> ${esc(r.service_location||'—')}</p><div class="private"><b>Private family/source contact</b><br>${esc(r.family_contact_name||'—')} • ${esc(r.family_contact_email||'—')} • ${esc(r.family_contact_phone||'—')}<br><b>Verification:</b> ${esc(r.verification_note||'—')}</div>${r.archived_at?`<p class="muted"><b>Archived:</b> ${esc(fmt(r.archived_at))} • ${esc(r.archive_reason||'')}</p>`:''}<div class="actions"><button class="ok" data-a="verify">Verify</button><button class="ok" data-a="publish">Publish</button><button class="neutral" data-a="needs_changes">Needs changes</button><button class="neutral" data-a="archive">Archive</button><button class="danger" data-a="reject">Reject</button><button class="danger" data-a="delete">Permanent delete</button></div></article>`}function render(){ $('count').textContent=`${rows.length} ${type==='lost_found'?'Lost & Found':'Memorial'} record${rows.length===1?'':'s'}`;$('grid').innerHTML=rows.length?rows.map((r,i)=>{const html=type==='lost_found'?cardLost(r):cardMem(r);return html.replace('<article class="mod-card">',`<article class="mod-card" data-i="${i}">`)}).join(''):'<article class="mod-card"><h3>No records found.</h3><p class="muted">New submissions will appear here when the production connection is configured.</p></article>';$('more').hidden=!cursor;document.querySelectorAll('.actions button').forEach(b=>b.addEventListener('click',()=>act(b)))}async function load(append=false){$('status').textContent='Loading…';const q=new URLSearchParams({type});if(append&&cursor)q.set('cursor',cursor);const r=await fetch('/api/admin-community?'+q,{headers:{authorization:`Bearer ${token}`}});const d=await r.json().catch(()=>({}));if(!r.ok){$('status').textContent=d.error||'Unable to load records.';return}const incoming=normalize(d);rows=append?rows.concat(incoming):incoming;cursor=pageCursor(d);$('gate').hidden=true;$('panel').hidden=false;$('status').textContent='';render()}async function act(btn){const card=btn.closest('[data-i]');const row=rows[Number(card.dataset.i)];const action=btn.dataset.a;const id=row.id||row._id||row.record_id;if(!id){alert('Record ID missing. Refresh and try again.');return}if(action==='delete'){const ok=confirm('Permanent deletion cannot be undone. Archive is recommended. Continue only for privacy, safety, legal, or explicit family/admin request.');if(!ok)return;const phrase=prompt('Type DELETE PERMANENTLY to confirm.');if(phrase!=='DELETE PERMANENTLY')return;btn.disabled=true;const r=await fetch('/api/admin-community',{method:'DELETE',headers:{'content-type':'application/json',authorization:`Bearer ${token}`},body:JSON.stringify({type,record_id:id,confirmation:phrase})});btn.disabled=false;if(!r.ok){alert((await r.json().catch(()=>({}))).error||'Delete failed');return}load(false);return}let reason='';if(['archive','reject','returned'].includes(action)){reason=prompt('Optional reason (for archive history):',action==='returned'?'returned':type==='memorial'?'service_complete':'other')||''}btn.disabled=true;const r=await fetch('/api/admin-community',{method:'PUT',headers:{'content-type':'application/json',authorization:`Bearer ${token}`},body:JSON.stringify({type,record_id:id,action,reason})});btn.disabled=false;const d=await r.json().catch(()=>({}));if(!r.ok){alert(d.error||'Update failed');return}load(false)}function switchType(next){type=next;rows=[];cursor='';$('tabLost').classList.toggle('active',type==='lost_found');$('tabMem').classList.toggle('active',type==='memorial');load(false)}$('open').addEventListener('click',()=>{token=$('token').value.trim();if(!token){$('status').textContent='Enter your admin token.';return}sessionStorage.setItem('pinoylink_admin_token',token);load(false)});$('refresh').addEventListener('click',()=>load(false));$('more').addEventListener('click',()=>load(true));$('tabLost').addEventListener('click',()=>switchType('lost_found'));$('tabMem').addEventListener('click',()=>switchType('memorial'));const saved=sessionStorage.getItem('pinoylink_admin_token');if(saved){token=saved;$('token').value=saved;load(false)}