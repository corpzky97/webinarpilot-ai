const $=id=>document.getElementById(id);$('memForm').addEventListener('submit',async e=>{e.preventDefault();const b=$('memSubmit'),s=$('memStatus');s.textContent='';if(!e.currentTarget.reportValidity())return;b.disabled=true;const old=b.textContent;b.textContent='Submitting…';try{const payload={full_name:$('fullName').value.trim(),birth_date:$('birthDate').value.trim(),death_date:$('deathDate').value,life_tribute:$('lifeTribute').value.trim(),service_details:$('serviceDetails').value.trim(),service_location:$('serviceLocation').value.trim(),family_contact_name:$('familyName').value.trim(),family_contact_email:$('familyEmail').value.trim(),family_contact_phone:$('familyPhone').value.trim(),public_contact:$('publicContact').value.trim(),donation_flower_url:$('donationUrl').value.trim(),source_type:$('sourceType').value};const r=await fetch('/api/memorials',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload)});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'Unable to submit memorial.');s.className='form-status full ok';s.textContent=`Memorial received${d.memorial_id?` — reference ${d.memorial_id}`:''}. It will be reviewed before publication.`;e.currentTarget.reset()}catch(err){s.className='form-status full err';s.textContent=err.message||'Unable to submit memorial.'}finally{b.disabled=false;b.textContent=old}});